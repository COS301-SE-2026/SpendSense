import { test } from '@playwright/test';

test.skip(
  true,
  'Complete this setup after choosing local Supabase or a controlled E2E browser session.',
);

test('creates the authenticated E2E browser storage state', async () => {});
