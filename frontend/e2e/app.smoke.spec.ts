import { test } from '@playwright/test';

test.skip(
  true,
  'Enable browser E2E tests after configuring the dedicated E2E browser session.',
);

test('browser E2E scaffold is ready for an authenticated journey', async () => {});
